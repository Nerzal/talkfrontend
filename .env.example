# Directory (or full URL) talks are loaded from at runtime via fetch().
# Must provide an index.json (array of talk IDs), a default-slides.md, and
# one <id>/talk.md per ID. Relative paths are served from the
# public/ directory.
VITE_TALKS_DIR=/talks
