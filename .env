# Ordner (oder vollständige URL), aus dem die Talks zur Laufzeit per fetch()
# geladen werden. Muss ein index.json (Array von Talk-IDs) und pro ID eine
# <id>.json (Talk-Objekt) bereitstellen. Relative Pfade werden aus dem
# public/-Ordner bedient.
VITE_TALKS_DIR=/talks
