# Directory (or full URL) talks are loaded from at runtime via fetch().
# Must provide an index.json (array of talk IDs) and one <id>.json
# (Talk object) per ID. Relative paths are served from the
# public/ directory.
VITE_TALKS_DIR=/talks
